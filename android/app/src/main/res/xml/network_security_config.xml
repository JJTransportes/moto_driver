<?xml version="1.0" encoding="utf-8"?>
<!--
    Restringe tráfego HTTP não criptografado (cleartext) apenas a hosts de
    desenvolvimento local. Todo o resto do app (produção) exige HTTPS.
    Ver F12 do relatório de auditoria — antes disso, o app usava
    android:usesCleartextTraffic="true" globalmente no AndroidManifest.xml.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
